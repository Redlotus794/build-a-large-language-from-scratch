# build-a-large-language-from-scratch
Implement a ChatGPT-like LLM in PyTorch from scratch, step by step

# Setup
```code
uv venv
source ./venv/bin/activate

uv pip install packages
uv pip install jupyter-book build
```